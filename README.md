# Galah Events

Static event preparation website for Galah Events in Rwanda.

## Preview

Open `index.html` in a browser, or serve the folder with:

```powershell
python -m http.server 4173
```

Then visit `http://localhost:4173`.
